// 1. Importar express y router

// 2. Importar topicController

// 3. Definir rutas GET(/), GET(/new), POST(/), GET(/:id), GET(/:id/edit), PUT(/:id), DELETE(/:id), POST(/:id/vote)

// 4. Exportar router
